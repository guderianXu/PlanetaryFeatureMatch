"""PlanetaryFeatureMatch 的 Python 单元测试包。

测试文件统一放在这里，避免继续污染 `python/` 业务代码根目录。
全量运行：
    PYTHONPATH=python:scripts python -m unittest discover -s python/tests -p 'test_*.py'
单个测试：
    PYTHONPATH=python:scripts python -m unittest python.tests.test_pfm_model
"""
