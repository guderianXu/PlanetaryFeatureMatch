# 发现记录

## PFM v2.1 architecture migration (2026-05-27)
- 本轮实现优先同步 PyTorch 训练/评估主路径，因为当前训练、报告、cache evaluator 和模型迭代都依赖 `python/pfm_model.py`；C++/LibTorch 推理端暂未同步，不能把新结构权重直接视为 C++ 推理可用。
- 旧 checkpoint 兼容需要两层处理：`graph_keypoint_meta_dim` 从 payload/config 中读取并 fallback 到 2；新增模块参数通过默认模型 state 自动补齐。这样旧 2 维 GraphMatcher keypoint projection 不会被新默认 4 维结构强行加载。
- C4 attention 更适合从各旋转分支 descriptor 自身产生 quality logits，而不是只从原始 P2 预测权重；实现上保留 branch-0 equivariant descriptor，再与 branch-quality fused invariant descriptor 拼接投影，避免完全旋转不变导致判别性进一步下降。
- texture branch 不能继续以 raw intensity 为主。局部归一化强度、DoG/LoG、梯度方向、ring contrast 与 soft census-like 特征更贴合行星影像的强光照、阴影和弱纹理退化。
- 自适应 texture gate 的初始输出必须接近旧行为，否则老 checkpoint 的评估口径会突变。本轮 gate 使用零初始化，让迁移后的默认初态基本等价于旧固定融合，然后由后续训练学习局部区域的 texture 权重。
- 训练报告必须同时保留 raw descriptor 与 GraphMatcher 口径。此前报告只看 raw descriptor，会低估正式模型链路；但只看 GraphMatcher 又会掩盖 descriptor 本身是否可靠，因此 `--report-matcher-mode both` 是后续训练诊断的默认推荐。
- 当前仍缺两块真正改变能力上限的实现：一是 GeometryHead 参与 descriptor canonical sampling，二是 weak-texture semi-dense fallback。当前代码已经完成稳定参数化、metadata 和报告接口，但这两块不应被误报为已经完整落地。
- 仿真数据继续训练验证了 raw descriptor/fusion 路线有效，但也暴露 GraphMatcher 训练缺口。`pose_sim2048_crop1024_v21lite_train3294_ctxfusion_b1p512_lr3e5_600_20260527` 将验证 Top1 从 0.6909 提到 0.8404，raw descriptor 抽样 precision 为 0.7362；同一 checkpoint 的 GraphMatcher precision 只有 0.5056，hard 组只有 0.0948。原因是当前 PyTorch loop 只训练 descriptor/fusion/context，不训练 GraphMatcher。
- 1024 crop + C4 四旋转分支 + blended descriptor fusion 的显存开销很高。batch4 和 batch2 都在第一步训练 OOM；batch1 + 512 points 稳定，训练显存约 19GB。已知的 gradient accumulation 实现会累计多 pair 计算图，不适合用来模拟大 batch。
- 当前可继续 checkpoint 全部是 v2-lite 配置（base 48、descriptor 192、graph hidden 384），不是新默认 full 256 维。不能直接用 `--init-random` 训练 full 256，因为现有 PyTorch fine-tune 默认冻结 backbone；如果要真正迁移到 full 256，需要增加 backbone/full-model stage 训练或做旧权重到新宽度的迁移策略。


## 模块边界整理 (2026-05-18)
- 最终目录结构：`modules/` 保留 runtime、logging、dataloader（基础部分）、core 四个通用模块；`src/` 包含 augment、cli、data、eval、geometry、infer、losses、models、train 九个项目特有模块。
- include 模式为模块相对路径（如 `"data/image_dataset.h"`），无需 `modules/` 或 `src/` 前缀，因此文件移动后只需在 CMake 中调整 include 目录，源文件基本无需修改。
- `synthetic_pair_dataset` 从 `modules/dataloader/` 移至 `src/data/`，因为它是项目特有的 online synthetic pair 生成逻辑，不属于通用 dataloader 基础设施。
- `pfm_augment` 保持为 OBJECT target 并入 `pfm`，链接关系不变。

## 模块化训练基础设施 / AsyncDataLoader
- 当前 reusable infrastructure 计划位于 `docs/superpowers/plans/2026-05-18-reusable-training-infrastructure.md`，设计位于 `docs/superpowers/specs/2026-05-18-reusable-training-infrastructure-design.md`。
- `runtime`、`dataloader` 的基础测试已存在并接入全量 `pfm_tests`。
- 全量测试曾在 `asyncDataLoaderFailedAsyncResetLeavesLoaderExhausted()` 崩溃；根因不是 BlockingQueue 本身，而是 `AsyncDataLoader::reset()` 在 `_sampler->indices()` 抛异常后提前退出，留下 `_queue == nullptr` 且 `_exhausted == false`，后续 `next()` 进入 async 分支调用 `_queue->pop()` 导致段错误。
- 最小修复是让 `reset()` 先进入安全 exhausted 状态并清空索引；只有 sampler 成功、async worker 启动成功后才把 `_exhausted` 置回 false。
- 修复后 `cmake --build build -j$(nproc) && ./build/pfm_tests` 通过，输出 `273 test(s) passed`。

## Training logging module
- 新增 `modules/logging/`，包括 `TrainingMetric`、`TrainingMetricLogger`、`ConsoleProgressLogger`、`CsvMetricLogger`、`NullGpuMetricProvider`、`MetricLoggerGroup`。
- `pfm_logging` 是独立 CMake target，并链接到 `pfm` / `pfm_tests`。
- 当前 logging module 还未集成进 trainer；下一阶段可用 `MetricLoggerGroup` 替换 trainer 里的直接 `std::cout`，并用 `CsvMetricLogger` 支持 `--log-csv`。
- logging module 实现后 `cmake --build build -j$(nproc) && ./build/pfm_tests` 通过，输出 `277 test(s) passed`。
- optional NVML provider 通过 `PFM_ENABLE_NVML` 控制；未找到 NVML headers/library 时仍编译 `NullGpuMetricProvider` fallback，避免普通开发环境被 GPU 指标依赖阻塞。

## Augmentation and synthetic pair dataset
- `modules/augment/` 已抽取 deterministic transform sampling 与 image-pair augmentation；`modules/data/synthetic_pair.cpp` 现在只负责旧配置到新配置的兼容转换。
- `pfm_augment` 需要作为 OBJECT target 并入 `pfm`，否则 augmentation 代码会反向依赖 `pfm` 中的 geometry/data 符号并产生静态库链接顺序问题。
- `SyntheticPairTensorDataset` 将内存中的 CHW float images 适配为 `TensorDataset`，按 `source_index = index / pairs_per_image`、`variant_index = index % pairs_per_image` 生成 `view_a/view_b/warp_a_to_b/valid_mask`。
- dataset adapter 实现后 `cmake --build build -j$(nproc) && ./build/pfm_tests` 通过，输出 `281 test(s) passed`。

## Trainer logging/DataLoader integration
- `TrainConfig::log_csv` 启用 per-iteration CSV metrics，列包含 feature/matcher/dense loss、descriptor accuracy/diversity、offset error，以及可选 GPU utilization/power。
- `--log-csv` 只增加一个高层日志输出参数，不暴露底层 logger 细节。
- `dataloader_workers > 0` 且未使用 synthetic pair cache 时，trainer 会预加载 source images 并通过 `SyntheticPairTensorDataset + AsyncDataLoader` 在线生成 pair；cache 路径保持原有同步读取。
- `pin_memory` 通过 DataLoader options 传递；无 CUDA/pinned-memory 支持环境下对应 dataloader 测试允许 runtime fallback 错误信息。
- trainer logging/DataLoader 集成后 `cmake --build build -j$(nproc) && ./build/pfm_tests` 通过，输出 `284 test(s) passed`。

## --min-keypoints 上下文
- `min_keypoints` 是软下限：只从有效强度掩码内、按分数补入原本被 NMS 抑制的候选，不突破 `max_keypoints`，候选不足时输出实际数量。

## 模型优化上下文 (2026-05-19)
- 当前模型由 Backbone、SparseHead、DenseHead、PlanetaryGraphMatcher 组成；默认结构参数文档为 base_channels=32、descriptor_dim=128、graph_hidden_dim=256、graph_attention_layers=4。
- 训练 loss 包括 repeatability、descriptor candidate CE、graph matching CE、dense smooth L1 offset、confidence BCE 和 descriptor diversity。
- 最近已完成 runtime/dataloader/logging 基础设施、online synthetic pair DataLoader、CSV 指标和训练可视化，适合先用指标驱动模型优化。
- 当前环境 `pfm-cf` 可编译并通过 `build-pfm-cf` 测试：284 test(s) passed，CTest 1/1 passed。

## Matching loss 不下降复现 (2026-05-19)
- 用用户命令的短版复现：epochs=3、batch=2、resize=512、pairs_per_image=15、mixed、min_keypoint_intensity=0.05、min_keypoints=1024、CUDA、CSV 指标。
- 指标证据：`graph_matching_loss` 首段均值约 3.9247，末段均值约 3.9361；epoch 1/2 均值均约 3.763，基本不降。
- 同一训练中 descriptor_loss 从首段均值约 2.717 降到末段约 0.295，descriptor_accuracy 从约 0.40 升到约 0.93；dense_loss 也从约 0.831 降到约 0.065。
- 代码证据：`PlanetaryGraphMatcherImpl::forward()` 接收 keypoints 但当前第 140-141 行显式 `(void)keypoints_a/b`，没有使用空间几何信息。
- 代码证据：`make_graph_matching_loss()` 用 256 个固定 descriptor 网格样本，B 侧只取 warp 后正样本集合，target 是 `0..sample_count-1`；监督问题更像“对一组正样本集合排序”，缺少真实关键点候选池和 dustbin/负样本结构。
- 初步根因假设：graph matcher 的输入/监督与实际 sparse matching 任务不一致，且未利用关键点几何，所以 loss 难以稳定下降；descriptor 分支能学会说明基础描述子监督不是主要瓶颈。

## Keypoint graph matching loss implementation findings (2026-05-19)
- 原 graph matcher 接口接收 keypoints 但实现忽略它们；加入 `_keypoint_projection` 后，same descriptors + different keypoints 会产生不同 logits。
- decoded `FeatureSet::keypoints` 是 sparse feature-map 坐标；`warp_a_to_b` 和 `valid_mask` 是 image-space。因此训练目标分配必须先把 keypoints 按 feature map 尺寸缩放到 image-space；graph matcher 本身仍应使用 inference 路径使用的 feature-map keypoints。
- `valid_mask` 由原始有效区域、A/B 强度 mask 共同构成；graph target assignment 必须同时检查 source 像素和 warped target 像素，否则会把无效目标监督为正样本。
- `decode_feature_maps()` 要求 dense confidence 与 sparse heatmap 空间尺寸一致；训练路径中 dense head confidence 需要 nearest resize 后才能作为 decode 输入。
- 完整 CUDA 短训练显示 graph loss 明显下降：4140 条 iteration 的 first_mean≈4.6967、last_mean≈0.0335，最后 10 条约 0.0010-0.0020；这与之前 fixed-grid graph loss 首末均值约 3.92/3.94 不下降形成对比。

## Full training results after keypoint graph supervision (2026-05-20)
- 完整 10 epoch CUDA 训练验证了 keypoint graph supervision 不只在短跑有效：13,800 条 iteration 中 graph_matching_loss 从首段均值≈2.17615 降到末段≈0.10262。
- 特征分支同步改善：descriptor_accuracy 从首段均值≈0.374187 升到末段≈0.95367；feature_loss 从≈2.78217 降到≈0.333686。
- dense 分支也稳定下降：dense_loss 从≈0.754952 降到≈0.0857591，offset_error_px 从≈156.817 降到≈16.3463。
- checkpoint `train_full.pt` 和训练诊断 `metrics_full.csv`/`vis_full/` 是本地训练产物，不应默认提交到 git。

## Inference filtering findings (2026-05-20)
- 训练 loss 收敛不等于推理 sparse match 可直接使用；如果推理忽略 dustbin 并强制每个 A keypoint 输出 argmax B，会在真实图像上产生大量低质量 sparse matches。
- Graph matcher 推理需要同时使用 dustbin 过滤和 mutual nearest 过滤：非 dustbin 说明模型认为该 A 有候选，mutual nearest 避免多个 A 单向挤到同一个 B。
- CUDA 推理路径必须按 matcher 参数设备搬运 feature tensors；features 本身由 decode 保持 CPU 是合理的，但 matcher 在 CUDA 时 forward 输入也必须到 CUDA。

## Trainer split integration finding (2026-05-20)
- 自动训练/验证划分的基础能力已存在于 `modules/dataloader/sampler`，但 trainer 曾重复实现 shuffle/slice；现在 trainer 复用 `make_train_validation_test_split()`，避免模块化 dataloader 能力闲置和两套划分逻辑漂移。
- `train_ratio + val_ratio` 隐含剩余比例作为 test split；当前 trainer 只使用 train/validation，test 保留给后续评估扩展。

## Extreme rotation matching finding (2026-05-20)
- 原 mixed/extreme augmentation 最大旋转约 ±55°，不足以训练 180° 旋转匹配；对 half-turn 图像对，模型倾向输出非交叉/同位置匹配是训练分布外行为。
- Graph matcher 的 keypoint projection 若使用原始像素/feature-map 坐标，会引入绝对位置偏置；对 180° 场景，绝对坐标相近反而通常是错误匹配。归一化坐标可减少尺寸尺度支配，但仍需重新训练来学习 half-turn 对应关系。
- 现有 eval half-turn 指标曾基于 `MatchSet.points_a/points_b`，但这些字段来自 semi-dense points，不是 graph matcher 的 `sparse_matches`；验证 sparse 关键点匹配必须结合 `FeatureSet.keypoints` 和 `MatchSet.sparse_matches`。
- 单图旋转 sweep 暴露更基础的问题：`train_full.pt` 在 `img/100.tif` 的 0° 自匹配上 sparse graph matcher 也不几何一致（60 条 sparse matches，3px 通过率 0，mean_error≈172.77 feature-map px）。因此当前问题不是只缺 180° 增强，而是 sparse matcher 推理/训练目标没有学到可靠的 identity/known-transform 对应。
- 30° step sweep 结果：0/30/60/90/150/180/210/240/270/300/330 度 3px 通过率均为 0；120 度仅 1/98 条通过。下一步应先修复 identity/known-rotation sparse matching，再做长训练。
- 训练 graph matcher 时裁剪 B 候选子集会改变 keypoint normalization 的上下文，和推理时完整 B set 不一致；改成 full B candidate 后，单图 0° 自匹配可达到 1024/1024 正确。
- 仅修 full B candidate、加入 ±90° augmentation、让 graph loss 回传到 sparse descriptor map，仍不足以让单图 90/180/270° 输出 sparse matches。当前证据指向更深的旋转不变描述子/关键点一致性问题，而不是单个 half-turn augmentation 缺口。
- descriptor 层诊断显示旋转后并非完全没有可重复关键点：`train_rotation100_graphgrad.pt` 在 90/180/270° 的 3px keypoint repeatability 约 54%-61%；但 raw descriptor mutual nearest 几何正确率只有约 0.8%-2.6%，且错误 mutual 的均值 cosine 约 0.976-0.978，高于真实几何重复点的均值约 0.86-0.88。这说明描述子在旋转后强混淆/近似塌缩。
- 网格 descriptor 全局对比和 sparse keypoint hard-negative CE 能让训练指标收敛，但单图 40 epoch 后 rotation sweep 仍失败；`train_rotation100_cfgdecode40.pt` 的 90/180/270° sparse matches 均为 0，descriptor mutual 几何正确率仍只有约 0.7%-3.7%。
- trainer 的 sparse decode 训练路径曾硬编码 `TrainConfig{max_keypoints=1024,min_keypoints=0,nms_radius=4}`，导致用户传入 `--min-keypoints 1024` 时 graph/sparse descriptor 训练看到的 keypoint 集合与推理不一致；已改为复用完整用户 `TrainConfig`。
- 当前更可信的根因：普通 CNN descriptor head 加旋转增强没有形成稳定旋转不变描述子；orientation/affine 头虽然存在，但未被监督也未参与 descriptor canonicalization/matching。下一步应把 orientation/affine 变为实用路径，而不是继续堆 descriptor CE。
- C4 descriptor pooling/statistics/harmonic bands 证明了“让真实重复点更相似”仍不够：`train_rotation100_harmonic.pt` 将 90/180/270° 真实重复点 descriptor cosine 提升到约 0.954-0.965，但错误 mutual nearest 的均值仍约 0.993，最终 sparse graph/fallback matching 仍全部拒配。
- 因此当前优化方向应从“更多旋转不变汇聚”转为“压低错误互近邻”：例如对 rotation sweep 生成的真实对应点做跨图 batch-hard margin/InfoNCE，或让 matcher 在多旋转/方向候选上显式比较并用几何一致性训练；仅靠单图内 descriptor diversity 指标不足以约束跨图旋转混淆。
- 扩大 sparse descriptor hard-negative 覆盖到 1024 queries 并把 margin weight 提到 5 后，`train_rotation100_margin5.pt` 仍无法匹配 90/180/270°：sparse matches 全为 0，descriptor mutual correct rate 约 0.6%-1.7%，错误 mutual score 仍约 0.990。这说明当前 invariant descriptor 空间里的 false nearest 问题不是简单加权可解。
- 接下来更值得做的结构性改动：不要过早把四个 C4 方向压成单个不变向量；应保留方向相关 descriptor 分量，让 matcher/descriptor similarity 在 4 个 cyclic shifts 上取最大或受监督选择正确方向。这样可以避免 pooling/harmonic magnitude 把不同地点的旋转纹理压到几乎相同的向量。
- 已验证上述 C4 cyclic slots + cyclic similarity 路线仍失败：训练收敛且 0° 自匹配正常，但 90/180/270° sparse matches 仍为 0，错误 mutual score 仍约 0.991。说明“方向槽位 + shift-invariant 比较”没有自动解决位置区分度，false nearest 仍是主瓶颈。
- 后续更可信的优化点应进入监督构造本身：用已知旋转生成的几何真值直接构造 all-keypoint/all-spatial batch-hard negatives，并约束 true positive score 必须高于同图/跨图最难负样本；或者显式监督 orientation，让 descriptor 在 canonical frame 中提取，而不是让网络从 CE 间接学会旋转规范化。
- keypoint-to-full-map descriptor 监督仍未解决极端旋转。重建 `pfm_cli` 后的可信结果是：`train_rotation100_keydense.pt` 的 90/180/270° sparse matches 分别为 92/108/87，但几何通过率只有约 1.1%/2.8%/2.3%，mean error 约 150-164 feature-map px。这说明“把 sparse keypoint descriptor 对齐到完整 B descriptor-map 真值位置”仍不足以压低跨旋转 false nearest。
- `train_rotation100_keydense.pt` 还让 repeatability 从 dense-hard 版本的约 55%-59% 降到约 40%-47%，提示当前强 descriptor 约束可能在和关键点稳定性/热力图目标竞争。后续若继续这条线，应同时加强 keypoint repeatability 几何监督，而不是只加 descriptor loss。
- rotation sweep 评估必须确保 `pfm_cli` 目标也被重建；仅构建 `pfm_tests` 和 `pfm_rotation_sweep_eval` 会留下旧 CLI 二进制，从而把已实现的 descriptor fallback/cyclic matching 误判为 0 sparse matches。
- orientation-supervised canonical descriptor 不是银弹：`train_rotation100_orientcanon.pt` 的 90/180/270° 几何通过率只有约 5.3%/3.1%/5.3%。它改善了 repeatability（约 53%-64%）并略微降低 false mutual score（约 0.977-0.983），但训练 feature loss 停在约 4.74，说明 canonicalization/orientation 监督和现有 descriptor CE 尚未协调好。
- 用户指出 180° 可视化仍接近平行是正确诊断：模型输出没有学到 half-turn 几何。训练数据的 warp 本身不是反的，但旧 mixed half-turn anchor 被 scale/translation 污染，且占比只有 1/8；这会让 descriptor mutual fallback 更容易保留近坐标/相似纹理匹配，而不是学中心对称 X 形对应。
- 改进后的 mixed profile 对 `variant % 8 == 3/7` 使用干净 ±90°/±180° 几何 anchor，避免极端旋转监督同时混入平移、缩放和强 photometric 变化。后续需要基于这个数据修复重新训练，旧 `train_rotation100_orientcanon.pt` 不代表修复后的训练分布。
- clean anchor 重新训练后仍失败，说明问题不只是 half-turn 样本被 scale/translation 污染：`train_rotation100_cleananchors.pt` 在 180° 上 pass rate≈2.42%，可视化仍是平行线。
- 单图训练原先每个 epoch 重复同一组 `pairs_per_image` variants，等价于在一个图上反复看同 8 个变换；这会高估训练进展且限制旋转/纹理覆盖。现在 variant index 随 epoch 前进。
- Graph matcher 使用 normalized x/y keypoint embedding 时，会给 180° 任务留下“同屏幕位置”捷径。改成 radius/radius^2 后能去掉明显绝对方向偏置，但 `train_rotation100_epochvariants_radialmatcher.pt` 的 180° pass rate 仍只有≈3.64%，说明主要瓶颈仍是 descriptor/局部纹理混淆。
- 当前最可信判断：继续盲训现有 CNN descriptor/head 收益很低。下一步应先在更快机器上用 SIFT/ORB 建立 180° baseline；如果传统局部描述子能给出 X 形匹配，就应把 learned feature extractor 改成真正的 orientation-normalized local patch descriptor 或 rotation-sweep hard-negative 监督。

## Rotation matcher benchmark tool (2026-05-25)
- 独立 Python benchmark 更适合做算法横向对比：它直接从 `img/1.tif` 与 `img/20260514T064636672_NAS_PAN_L2b.tif` 读取图像，先可选缩放，再用 `np.rot90` 构造 90/180/270 旋转对，并用同一套真值坐标计算 `matches/correct/wrong/precision/mean_error_px/median_error_px`。
- `np.rot90(k=1)` 是逆时针旋转，点坐标真值为 90° `(y, width-1-x)`、180° `(width-1-x, height-1-y)`、270° `(height-1-y, x)`；真实 smoke 中 180° 全对但 90/270° 全错是该公式写反导致。
- 当前 `pfm-train` 环境中 OpenCV 与 PyTorch 可用，LightGlue Python 包可探测但本项目没有本地 adapter，SuperGlue 常见 `match_pairs` 入口不存在。benchmark 会将这些可选 matcher 写为 `unavailable`，不会中断 SIFT/ORB/AKAZE/PFM。
- `runs/rotation_matcher_benchmark_20260525/metrics.csv` 显示在长边 768、每组最多 256 matches 的同图旋转 smoke 中，SIFT/ORB/AKAZE 均为 18/18 个图像-角度组合满 precision；PFM 除 timestamp 180° 为 255/256 外，其余为 256/256。该结果验证工具和真值计算，不代表跨视角泛化已解决。
- 旁路迭代入口确认：`python/test_rotation_matcher_benchmark.py` focused 测试在 `pfm-train` 环境通过，已有 `runs/rotation_matcher_benchmark_all_learned_full_20260525/summary.txt` 只记录 available/unavailable，不含聚合表；详细聚合需从 `metrics.csv` 计算。
- RootSIFT-FLANN-RANSAC 在 `runs/rotation_matcher_iter_rootsift_ransac` 的两类图像 x 90/180/270 上为 1536/1536 correct，precision=1.0；这没有超过 SIFT/ORB/AKAZE/PFM 的 ceiling，但补充了显式 ratio+mutual+RANSAC 几何过滤传统 baseline。
- 本地 OpenCV 4.13.0 提供 `cv2.AffineFeature_create`，可直接封装 `SIFT_create()` 为仿射模拟检测器。`AffineSIFT-BF` 在 `runs/rotation_matcher_iter_affinesift` 为 1536/1536 correct，precision=1.0；它验证仿射模拟 SIFT 在该离散旋转 smoke 上同样达到 ceiling，但没有拉开传统 baseline 差异。
- 继续迭代前确认：当前 benchmark 口径是两种图像风格各 90/180/270，同一套旋转真值计算 `matches/correct/wrong/precision`；`runs/rotation_matcher_iter_rootsift_ransac` 与 `runs/rotation_matcher_iter_affinesift` 中 RootSIFT-FLANN-RANSAC、AffineSIFT-BF、SIFT/ORB/AKAZE/PFM 均达到 1536/1536 correct。
- `pfm-train` 环境 OpenCV 为 4.13.0，提供 `cv2.USAC_MAGSAC=38`、`cv2.USAC_PROSAC=37`、`cv2.USAC_DEFAULT=32`，可作为 RootSIFT-FLANN 后的几何验证稳健性补充，不需要额外下载或训练。
- OpenCV 4.13.0 虽然暴露 USAC 常量，但 `cv2.estimateAffinePartial2D()` 对 `USAC_MAGSAC/USAC_PROSAC` 报 `Unknown or unsupported robust estimation method`；可用实现是 RootSIFT/FLANN 后改用 `cv2.findHomography(..., method=USAC_*)` 做稳健内点过滤。
- `runs/rotation_matcher_iter_continued_usac_20260525` 结果：RootSIFT-FLANN-USAC-MAGSAC 与 RootSIFT-FLANN-USAC-PROSAC 均为 1536/1536 correct、precision=1.0，和 RootSIFT-FLANN-RANSAC、AffineSIFT-BF、PFM 同处该 benchmark ceiling。

## Cross-view 1024 training/eval loop findings (2026-05-26)
- 当前 `pytorch_cache_match_eval.py` summary 列是 `pair_pt,matches,correct,wrong,precision`；旧 `hard_pair_mining.py` 只读 `sparse_matches/match_precision`，会导致从当前 summary 选不出 hard pairs。修复后 hard-pair mining 同时支持两套列名。
- `--training-groups` 对 specialist 训练是必要基础设施：只限制训练/validation cache dirs，不改变六组 test evaluation。这允许把单 gate checkpoint 作为路由候选验证，而不是误认为它应该替换所有 groups。
- `runs/cross_view_1024_timestamp_viewpoint_specialist_60_seed1234` 说明短程单 gate descriptor-only finetuning会偏向低召回高 precision：numeric/rotate 和 timestamp/rotate precision 看似升高，但 matches 大幅减少；timestamp/viewpoint 只从 13/212 (0.061321) 到 9/136 (0.066176)。后续目标应显式约束 match support/recall 或改 loss，而不是继续同样的单 gate 短训。
- `--calibration-pytorch-state` 把 checkpoint 也纳入 validation calibration 后，`runs/cross_view_1024_checkpoint_routed_margin_multiseed16_min20_0step_seed1234` 六组全部选择了 `blend02540` checkpoint，但 fixed 64-pair test 暴露了过拟合小 validation precision 的问题：numeric/rotate、numeric/compound、timestamp/rotate precision 提升，numeric/viewpoint 低于当前最佳 margin run，timestamp/viewpoint 基本不变，timestamp/compound 退到 0/23。checkpoint routing 必须加入 match support/recall 或 full-val/full-test safeguard，不能只按小样本 precision 贪心选择。
- state-switch guard 证明了“checkpoint 路由要带收益门槛和支撑门槛”是有效的：`runs/cross_view_1024_checkpoint_routed_guard_frac010_gain003_ratio025_0step_seed1234` 只允许 numeric/rotate、timestamp/rotate 切到 `blend02540`，其余组回退 trained；它保留 rotate precision 提升并恢复 timestamp/compound 到 11/244。该结果说明 routing 层可以稳住已知 specialist 的收益，但 timestamp/viewpoint 的瓶颈仍不在 checkpoint 选择，而在 descriptor/keypoint 对跨高度 viewpoint 的召回和正确性。
- 独立旋转 matcher comparison 进一步确认：同图 90/180/270 旋转不是当前 PFM 的弱项，PFM、SIFT、ORB、AKAZE 以及 RootSIFT/USAC 传统变体都能达到 1536/1536 correct；弱的是默认 learned local features（SuperPoint/DISK/LoFTR）在大离散旋转上的泛化。因此 cross-view 1024 的低 precision 更像跨高度/视角/纹理域问题，而不是简单的离散旋转不变性问题。
- Hard-mined weak-gate descriptor training is not sufficient with the current objective. `runs/cross_view_1024_hard_mined_weakgates_80_seed1234` mined genuinely hard train pairs, but 80-step hard-pair curriculum worsened descriptor retrieval and test precision. The post-selected base-reference run recovered the previous guarded weak-group metrics but did not improve them. This points away from “sample harder synthetic pairs with the same descriptor CE” as the next main route.
- Checkpoint state-switch safeguards need an explicit reference label when the current run produces a new experimental checkpoint. Without `--calibration-state-switch-reference-label base`, the new checkpoint is called `trained` and becomes the fallback reference, so base/blend candidates must exceed it instead of the stable base checkpoint. The new CLI parameter fixes this ambiguity for future multi-checkpoint experiments.
- Actual cross-view traditional matcher comparison changes the diagnosis: RootSIFT-FLANN-ratio raw precision on sampled real cache pairs is 0.830565-0.885888 across numeric/timestamp viewpoint/compound, and RootSIFT + homography RANSAC reaches 0.972522-0.998124 precision. Current PFM guarded summary on the same sampled groups is only 0.104478-0.361702. Therefore the learned model is not limited by impossible geometry or unavailable signal; it is failing to learn/retain the correspondences that classical local features already find. The next high-value training change should use RootSIFT/H-RANSAC inliers as pseudo-labels or distillation targets, rather than only optimizing synthetic warp descriptors.
- Pseudo-label CSV support must be paired with explicit pseudo-labeled pair sampling. The first CSV had 24 labeled pairs inside a much larger weak-gate train pool, and a 40-step run sampled zero pseudo-label points for every step. A curriculum probability over exact pseudo-labeled pair paths is required for reliable training signal.
- Mixing synthetic warp descriptor CE with RootSIFT pseudo-label CE can erase the pseudo-label benefit in short weak-gate runs. Runs with synthetic loss enabled or pseudo-label weight too low regressed validation retrieval even after consuming labels. The first positive results came from `--synthetic-loss-weight 0` / pseudo-label-only updates, suggesting the classical inliers are a different and cleaner target than the existing dense synthetic warp sampling objective.
- RootSIFT pseudo-label thresholds should be tighter than the initial Agent3 comparison threshold when used for training. Agent5/Agent6 both point to `ratio=0.80` and homography RANSAC 2px as the clean default; 3px adds labels but lowers precision. Pair-level filters should preserve high precision, e.g. precision >=0.98 and group-aware min inliers.
- Expanded r0.80/t2 pseudo-only training gives a small but real descriptor retrieval improvement on the weak-group validation split: top1 0.1004 -> 0.1027, top5 0.2859 -> 0.2911, loss 4.5366 -> 4.5266. This is not yet proof of better sparse matching precision; it must be checked through the six-group calibrated evaluator.
- The six-group calibrated evaluator confirmed that the pseudo-only checkpoint transfers specifically to `timestamp/viewpoint`: calibrated test precision improved from the old guarded route's 13/212 (0.061321) to 23/249 (0.092369). It did not improve the other groups enough to pass guarded state switching. Therefore pseudo-label descriptor training should be treated as a timestamp/viewpoint specialist candidate, not a global replacement checkpoint.
- Future checkpoint routing should include all proven specialists simultaneously. The pseudo guarded eval compared only pseudo vs base, so rotate fell back to base and lost the older `blend02540` rotate gains. The best current routing evidence is a composite: old guarded route for rotate/compound groups, pseudo checkpoint for timestamp/viewpoint.
- Current PFM matches are not safe as pseudo-label fallback, even after geometry filtering. Agent7 measured raw PFM precision 322/14599 (0.0221) on rotated cross-view samples; Agent8's best global PFM+homography filtering reached only 28/46 (0.6087), with no global precision >=0.80 configuration. Keep PFM out of pseudo-label mining until the matcher confidence/geometry layer changes substantially.
- RootSIFT r0.90/t2 is useful only as an insertion fallback, not a replacement for r0.80/t2. Agent10's larger sample showed r0.90/t2 recovers 19/65 r0.80/t2 gate failures at 584/589 fallback precision (0.9915), but its all-sample precision drops to 0.9695 versus r0.80/t2 at 0.9935. This supports fallback-only mining for extra labels, not full r0.90 label replacement.
- More pseudo-label coverage improves descriptor retrieval but does not automatically improve sparse matching. The 160-step group-aware r0.80/t2 run improved weak-group validation top1 from 0.0784 to 0.0822, but guarded sparse evaluation did not beat the earlier smaller r0.80/t2 timestamp/viewpoint specialist. The model can fit descriptor retrieval signal without producing better calibrated sparse matches.
- Full r0.90/t2 and r0.80+t0.90-fallback training are negative for route selection. Full r0.90/t2 fixed sparse eval slightly improved timestamp/viewpoint to 24/256 but regressed numeric/viewpoint to 134/433; fallback-only training was weaker at timestamp/viewpoint 20/230. Timestamp/viewpoint-only r0.90 specialist reached only 14/165. The stable best sparse training-side result remains the earlier r0.80/t2 pseudo specialist at 23/249 for timestamp/viewpoint.
- Keypoint heatmap distillation only affects current sparse matching if evaluation explicitly uses the learned heatmap for keypoint ranking. The default sparse evaluator historically selected keypoints from image texture; adding `--keypoint-score-mode learned` is required to test whether heatmap supervision changes match behavior.
- The first r0.80/t2 descriptor+keypoint run is not a better descriptor model: viewpoint validation retrieval regressed from top1 0.1947/top5 0.4432 to top1 0.1878/top5 0.4255. However, learned heatmap ranking improved the fixed timestamp/viewpoint sparse sample when paired with strong descriptor filtering: learned score + blend 4 + margin 0.01 reached 41/223 (0.183857), above the old r0.80/t2 pseudo specialist's 23/249 (0.092369). This is a specialist routing signal, not a global checkpoint signal, because numeric/viewpoint did not improve and support remains limited.
- Agent11 confirms the external matcher diagnosis under rotated cross-view conditions: RootSIFT-r0.90-Ht2 and LightGlue-SIFT-Ht3 reached near-perfect precision on numeric/timestamp viewpoint/compound samples with 90/180/270 rotation, while PFM raw remained at 83/2161 (0.038408). The next high-value model work should either distill these external matchers into keypoint/descriptor heads or route to them, not use PFM outputs as labels.
- Learned keypoint scoring should be treated as a calibrated route dimension, not a fixed replacement for texture keypoint ranking. In the first six-group learned-score calibration, the selected routes used learned scoring in 5/6 groups, but the winning checkpoint was often base or blend rather than the newly trained heatmap checkpoint.
- Freezing descriptor behavior and training only the heatmap head is safer than joint descriptor+keypoint training. The viewpoint keypoint-only and weakgroups keypoint-only runs kept descriptor validation exactly unchanged, while still changing sparse matching route candidates through `--keypoint-score-mode learned`.
- The weakgroups keypoint-only run produced only a modest compound gain. It allowed the trained heatmap checkpoint to pass guarded selection for numeric/compound and timestamp/compound; timestamp/compound improved on the fixed sample from 3/108 (0.027778) to 5/72 (0.069444), while numeric/compound precision barely moved and correct matches fell from 49 to 47. This is useful evidence for heatmap distillation, but not yet a solved compound route.
- Timestamp/viewpoint did not improve from adding compound heatmap labels. In the guarded six-group setup it stayed at 20/104 (0.192308), selected from the base checkpoint with learned keypoint scoring. The next timestamp/viewpoint improvement likely needs a stronger keypoint confidence/routing target or full-validation-backed specialist route, not just more compound heatmap labels.
- Evaluation/calibration GPU utilization is expected to be low or bursty because the orchestrator launches many small pairwise evaluator subprocesses and does CSV/geometric filtering per candidate. Increasing training `batch_pairs` from 4 to 8 raised the training-step workload without changing descriptor metrics, but calibration remains Python/I/O dominated.
- Agent12's broader rotated cross-view sample confirms the same teacher hierarchy with stronger tuning detail: `RootSIFT-ratio-r0p90-Ht3-min4` gives the highest correct count and full coverage (759/794, 0.955919), while `LightGlue-SIFT-Ht3-min4` gives higher precision but slightly lower coverage (674/683, 0.986823, 11/12 covered). Current PFM remains too noisy for fallback labels (raw 20/974, filtered 7/71).
- The r0.90/Ht3 teacher should be used as a coverage-expansion candidate, not a replacement for conservative r0.80/t2 descriptor training. Earlier r0.90/t2 descriptor-side runs regressed route selection; the safer next use is heatmap-only distillation where descriptor validation is preserved and routing can reject weak groups.
- The r0.90/Ht3 heatmap-only coverage experiment validated that caution. It preserved descriptor validation and improved numeric/compound on the fixed sample to 50/225 (0.222222), but timestamp/compound regressed to 3/109 (0.027523), below the r0.80/t2 weakgroups heatmap result of 5/72 (0.069444). The higher coverage teacher changes keypoint ranking but is not uniformly better across styles.
- The current evidence supports style/gate-specific teacher selection: r0.90/Ht3 can be useful for numeric/compound heatmap ranking, while r0.80/t2 remains the better timestamp/compound heatmap teacher. A single global RootSIFT threshold for heatmap distillation is too blunt.
- Agent13 strengthens the teacher-mining direction: `RootSIFT-ratio-r0p88-Ht3-min4` improves the rotated cross-view teacher tradeoff over Agent12's r0.90/Ht3, with 779/801 correct/inliers (0.972534), 12/12 coverage, and 9/12 pass gates. It should be tested as a pseudo-label source before another training run, especially for timestamp/compound where Agent13 measured 39/39 and 3/3 pass gates.
- The first style-specific heatmap-label mix confirms that better teacher selection must be paired with multi-checkpoint routing. Numeric/compound improved to 50/224 (0.223214) with the style-specific checkpoint, but timestamp/compound fell to 3/111 when that same checkpoint was forced through single-checkpoint calibration. Adding prior specialists back into calibration restored timestamp/compound to 5/72 while keeping numeric/compound at 50/224.
- Current best heatmap-side route on the fixed 64-pair protocol is compositional: `blend02540` for rotate and numeric/viewpoint, style-specific heatmap checkpoint for numeric/compound, base for timestamp/viewpoint, and r0.80/t2 weakgroups heatmap checkpoint for timestamp/compound. This route is represented by `runs/cross_view_1024_keypointonly_multistate_stylespecific_guard_calib_0step_seed1234`.
- Do not collapse heatmap specialists into a single global checkpoint yet. The observed gains are group-specific and small; validation can select a candidate that fails on fixed test for timestamp/compound. Future runs should keep all proven specialists available in calibration and add full-validation or stronger support safeguards before declaring a route replacement.
- Agent13 stage2 shows that sampled train precision alone is an incomplete proxy for heatmap usefulness. The selected style-specific bundle had high aggregate train truth precision (0.9874) and better numeric/compound teacher quality, but its heatmap-only checkpoint still produced timestamp/compound 3/107 on fixed test. This suggests the timestamp/compound issue may be label coverage/distribution, learned heatmap calibration, or train/test source mismatch rather than simply noisy teacher labels.
- Pseudo-label path keys need care with split symlinks. Absolute paths under `runs/.../splits/train/...` may not match because the training helper resolves symlinks to `img/...`; relative split paths match the pair paths collected by the trainer. Future generated CSVs should either use repo-relative split paths or include duplicate rows for both split and resolved image paths.

## PFM v2.1-full architecture landing findings (2026-05-27)
- The previous `PFM v2.1 架构迁移` state was only a partial landing: it had C4, texture fusion and stable geometry heads, but not Dual-FPN, descriptor canonical pooling, QualityHead, GraphMatcherV2 rich metadata/top-k/bias, or matcher training loss.
- The PyTorch main path now owns the full v2.1 architecture. New random models default to full capacity: `base_channels=64`, `descriptor_dim=256`, `graph_hidden_dim=512`, `graph_attention_layers=8`, `graph_keypoint_meta_dim=16`.
- Existing v2-lite checkpoints remain intentionally compatible. Loading a 48/192 checkpoint still instantiates the checkpoint shape, so it will not silently become a 256-dimensional model. A true full model requires random/full initialization or a separate width-migration strategy.
- GraphMatcherV2 now accepts richer metadata. Evaluation currently supplies x/y plus score/quality when available; the model-side metadata format also supports log scale, orientation, affine, local contrast and uncertainty for training paths that can provide them.
- The new `--train-graph-matcher` flag is meaningful: `pfm_pytorch_training.py` now adds a graph matcher correspondence cross entropy from known warp correspondences instead of merely marking matcher parameters trainable.
- Verification after the landing passed: focused tests `128 OK, 1 skipped`, full Python discovery `223 OK, 2 skipped`, and small forward/matcher smoke succeeded.
- A full CLI smoke with random v2.1-full initialization and all major train flags completed one CUDA step on real generated cache at 128px scale. This proves the new architecture is wired through model loading, optimizer parameter selection, descriptor loss, graph matcher loss, checkpoint saving and metrics writing.
- The architecture gap that mattered most after the first v2.1-full landing was not C++ or training duration; it was that GraphMatcher evaluation still received only partial metadata and the semi-dense branch was only a fallback behavior. This is now fixed in the PyTorch architecture: `SemiDenseCandidateBranch` generates detector-free coarse candidates, and GraphMatcher metadata is sampled from `RawFeatureMaps` for scale/orientation/affine/quality/local contrast.
- The remaining gap is no longer the model architecture itself, but training maturity: the new semi-dense branch and richer GraphMatcher metadata have executable paths and tests, but they still need full-scale training before their weights are meaningful.
- LightGlue-SIFT is a promising numeric/compound teacher but not yet proven as a general heatmap teacher. Stage2 train mining selected it at 37 kept pairs / 3704 labels / 0.9968 truth precision for numeric/compound, and the resulting checkpoint matched the current numeric/compound best 50/224. It did not improve other groups when forced as part of a single checkpoint.
- Agent13 stage3 indicates timestamp/compound is a hard-tail coverage problem, not a simple RootSIFT threshold precision problem. r0.88/Ht3 kept 103/128 timestamp/compound train pairs at 0.9846 truth precision, but it largely overlapped r0.80/r0.90 kept pairs and left 24/128 pairs with too few truth labels. Newly recovered pairs were much weaker than common kept pairs, so more of the same RootSIFT gate mostly adds easy-pair density.
- The balanced plus hard-tail heatmap run is a negative result. Capping stage2 labels to 32 per pair and adding RootSIFT r0.88/Ht3 hard-tail leftovers produced only 33 extra hard-tail labels and evaluated at timestamp/compound 3/112, below the existing r0.80/t2 weakgroups heatmap route of 5/72. Hard-tail mining needs a stronger or different teacher, not another cap-only r0.88 label mix.
- The next matcher-side iteration should specifically test whether LightGlue-SIFT, looser-but-truth-filtered RootSIFT, CLAHE-RootSIFT, or other fallbacks can cover the timestamp/compound hard-tail pairs. A candidate with lower raw gate precision may still be useful only if truth filtering yields enough labels across currently uncovered pairs; otherwise it should stay diagnostic and not enter heatmap training.
- Exhaustive all-specialist checkpoint routing is too expensive for quick iteration at the current candidate granularity. A wide matrix with base/blend/r0.80/r0.90/style/stage2/balanced states, five blend weights, two margins, two score modes, and multi-seed validation advanced too slowly; even the narrowed 6-state, 3-weight, one-margin, two-score-mode run was still slow. Future route triage should reuse known per-group parameter settings or evaluate one new checkpoint against the current selected route instead of sweeping every axis.
- Agent13 stage4 shows that the remaining timestamp/compound hard-tail is not recovered by ordinary sparse local-feature variants. Among 24 hard-tail pairs, only one pair produced high-precision candidate labels, and both useful teachers labeled the same pair. The strongest non-diagnostic output was CLAHE-RootSIFT r0.92/Ht3 with 26 labels; LightGlue-SIFT added 12 labels; RootSIFT r0.88/Ht3 kept zero. This confirms the problem is not just choosing r0.88 versus r0.92/r0.95.
- Because Stage4 only added 38 labels from one pair, it should not be appended to the heatmap pseudo-label training set as a supposed hard-tail fix. The next meaningful teacher search should switch matcher family or image context, e.g. LoFTR/dense matching/larger crop context, and still require warp-truth precision gates before training.
- Stage5 confirms that switching matcher families is still not enough for the timestamp/compound hard-tail. LoFTR outdoor, larger LightGlue-SIFT budgets, Farneback, and DISFlow together cover only 2/24 unique hard-tail pairs, with only one new pair beyond Stage4. Dense flow produced many homography inliers but zero warp-truth labels, which indicates the cached hard-tail pairs are not simply missing sparse detector coverage; their local appearance/warp truth is not producing reliable correspondences under these teachers.
- The practical implication is to stop treating the 24 Stage3 hard-tail pairs as a pseudo-label mining target. They should either be excluded/downweighted from heatmap teacher training, regenerated with larger context or different crop geometry, or handled by route/fallback policy. Adding 416 Stage5 labels would mostly overweight one already-covered pair and one new pair, likely worsening source/pair imbalance.
- Cache-level data diagnostics rule out a simple overlap/valid-mask explanation for timestamp/compound hard-tail failure. Hard-tail pairs have higher median valid-mask fraction and full target-inside warp support compared with kept pairs. Their B-view gradient is lower, which is consistent with target-view texture/repeatability loss. This supports moving to larger-context target crops, source/pair re-selection, or route/downweight policy rather than more local matcher pseudo-label mining.
- Stage6 quality-gate analysis confirms that route-level abstention is a better immediate use of the hard-tail evidence than more sparse teacher mining. For current timestamp/compound, B-view local contrast is the strongest diagnostic feature: validation precision can improve from 12/208 (0.057692) to roughly 10/91-95 (0.105-0.110) while retaining 10/12 correct matches. B-view gradient is also stable but slightly weaker.
- Direct evaluator thresholds should be calibrated on `.pt` tensors, not copied blindly from PNG diagnostics. Stage6's PNG threshold `image_b_local_contrast_mean >= 5.323792` gives good full-val precision but loses one extra fixed-test correct under tensor evaluation. A conservative candidate around `--min-target-local-contrast 5.2` is more balanced on the current evidence: full-val 10/108 (0.092593), fixed-test 4/45 (0.088889).
- Target-view quality gates are routing/abstain features, not model improvements. They improve precision by refusing low-repeatability B-view pairs, so future route reporting must show both precision and correct-match retention/support. They should be calibration candidates with support/retention safeguards, not a hard-coded global rule.
- The first six-group target-contrast postselected route confirms the tradeoff: timestamp/compound improves in precision from 0.069444 to 0.088889, but loses one correct match on the fixed sample (5 -> 4). This is useful for high-precision reporting, but it is not yet a strict replacement for the previous best route unless a fallback recovers the dropped correct match or the user prioritizes precision over correct count.
- Stage7 resolved the fallback question for the current contrast gate: the abstained timestamp/compound pairs still contain strong classical local-feature signal. `RootSIFT-FLANN-r0.80+HomographyUSAC-t2` recovered the dropped fixed-test set at 890/898 (0.991091) and full-val dropped set at 3562/3577 (0.995807), yielding combined timestamp/compound precision 894/943 (0.948038) on fixed test and 3572/3685 (0.969335) on full val.
- The combined contrast + RootSIFT result must be reported as a hybrid route, not as learned PFM progress. It is still useful: it proves the low-contrast abstained pairs are not geometrically impossible, and it gives a high-confidence teacher/fallback policy. The model-side interpretation is to distill or pseudo-label those recovered pairs, not to claim the PFM checkpoint learned them.
- The next matcher-side question is policy scope. Stage8 has been dispatched to test whether the same external fallback should remain only on timestamp/compound abstentions or whether per-style/per-gate fallback helps other fixed-test groups. This should not block the main training loop and should not edit core training/eval files.
- Low target-local-contrast training pairs are not uniformly hopeless. In the timestamp/compound train split, 824/1536 pairs have B-view local contrast < 5.2, and a 128-pair sample still yielded 88 trainable pairs with 8317 strict RootSIFT r0.80/H2/truth2 labels. This is a better training target than the earlier Stage4/5 hard-tail set, which only covered 1-2 unique hard-tail pairs.
- A targeted low-contrast heatmap continuation gives a small but useful pure-PFM signal for timestamp/compound. Unlike the pure abstain gate, it retained the fixed-test 5 correct matches while reducing false matches from 67 to 54. When combined with the same target-contrast gate, it improved fixed-test PFM-only timestamp/compound from 4/45 to 5/36 and full-val from 10/108 to 12/126. This should enter the next multi-state calibration as a candidate checkpoint.
- The effect size is still small and localized. The low-contrast checkpoint has only been checked on timestamp/compound; it has not been cleared for the other five groups. It should not replace the current multistate route until a six-group guarded route sweep confirms that it is selected only where beneficial.
- Stage8 reframes the external fallback policy: targetcontrast gate-zero rows across all six fixed-test groups often have strong classical correspondences, not just timestamp/compound dropped rows. RootSIFT r0.80/H2 fallback on all gate-zero rows produced 60108/60431 correct fallback matches and improved every group when measured as a hybrid policy.
- The all-gate-zero hybrid route is valuable for deployment/fallback and teacher mining, but it is not a model improvement. Its huge match counts come from an external classical matcher filling PFM abstentions. It must be reported separately from pure-PFM route metrics and validated on held-out/full-val before replacing the narrower timestamp/compound-only fallback route.
- For model training, Stage8 suggests a broader distillation target: pairs where PFM emits zero but RootSIFT has high-confidence, warp-valid correspondences. This may be useful for future heatmap/keypoint distillation beyond timestamp/compound, but training should start from train-split labels and guarded route validation, as with the low-contrast checkpoint.
- Lowcontrast heatmap training is a timestamp/compound specialist, not a numeric/compound replacement. With the current numeric/compound selected params, the lowcontrast checkpoint produced 204/2725 (0.074862) on fixed test, far worse than the existing style-specific numeric/compound route. The six-group pure-PFM postselected route should therefore use lowcontrast only for timestamp/compound.
- The lowcontrast + target-contrast route gives the best pure-PFM timestamp/compound precision so far on the fixed protocol: 5/36 (0.138889), while retaining the previous best route's 5 correct matches. It remains a postselected precision route, not a full all-axis calibration result, but it is stronger evidence than the earlier abstain-only route because correct count improves from 4 back to 5.
- Matcher-side policy validation is now delegated to Stage9. Until Stage9 validates all-gate-zero fallback on full-val/held-out data, keep the broad RootSIFT all-gate-zero route as an external hybrid candidate and keep pure learned PFM metrics separate.
- Stage9 and Stage10 both validate a stricter broad external fallback: RootSIFT r0.75/H2 has lower support than r0.80/H2 but consistently fewer wrong matches and higher precision on held-out and full-val. Stage10 full-val r0.75/H2 fallback is 183662/184456 (0.995695), while r0.80/H2 is 195168/196531 (0.993065). This supports using r0.75/H2 as the high-precision hybrid default and retaining r0.80/H2 only when recall/support is the priority.
- Fixed-test Stage11 confirms the same tradeoff: r0.75/H2 broad hybrid improves precision from Stage8's 0.986860 to 0.988985 and reduces wrong matches from 818 to 650, but correct matches drop from 61437 to 58362. This is a policy choice, not a pure PFM model improvement.
- Broad gate-zero heatmap distillation is a negative pure-PFM result. Train-only r0.75/H2 gate-zero labels produced 6844 labels across 115 pairs and a heatmap-only checkpoint, but full-val precision collapsed on both compound groups despite small fixed-test gains. This suggests PFM zero-match teacher labels are too broad for direct heatmap activation without a stronger confidence/retention objective.
- Full-val safeguards matter more than fixed-test deltas for tiny pure-PFM compound gains. The gate-zero checkpoint looked slightly better on fixed-test numeric/compound and timestamp/compound, but full-val contradicted both; do not add it to multi-state routing.
- Stage12 narrows the safe distillation direction: do not convert broad external fallback into dense heatmap positives. The only recommended next training probe is a tiny, pair-filtered, noncompound viewpoint correspondence/descriptor retention probe with strict caps and hard negatives. Compound groups remain high over-activation risk until a viewpoint-only probe proves full-val non-regression.
- Timestamp/viewpoint target-quality gates improve precision but reduce support. Local contrast 5.2/5.6 and target gradient 20 all raise fixed64 precision over the 20/104 baseline, but lose 3-7 correct matches; unlike timestamp/compound, there is not yet a quality gate that both improves precision and preserves correct-match count. Treat this as a precision-only reporting option, not a default pure-PFM route change.
- There is currently no official installed or installable PPT/PowerPoint/slides skill in the checked `openai/skills` sources. For presentation deliverables, `python-pptx` is a practical local fallback and can embed charts plus existing match visualizations from `runs/`.
- Presentation-related plugin skills were present only under the temporary plugin cache (`sharepoint-powerpoint`, `canva-branded-presentation`) and not as active local skills for this workspace. The generated project PPT follows their practical workflow where applicable: gather source evidence, build a slide plan, use a repeatable local `.pptx` generator, and verify the deck through LibreOffice rendering.
- P1 pair-filtered descriptor retention is safer than broad heatmap activation but still not sufficient for route inclusion. The both-viewpoint P1 probe used clean labels (744 labels, wrong=0), yet numeric/viewpoint full-val over-activated from 836 to 2698 matches and precision fell from 0.443780 to 0.351742. This shows even tiny correspondence labels can loosen descriptor matching unless the objective also preserves abstention/precision.
- Timestamp/viewpoint has a fragile descriptor-retention signal: both-viewpoint P1 improved full-val from 78/389 to 81/371, and timestamp-only P1 improved full-val to 83/398, but both failed fixed64 transfer. Treat these as evidence that train-only r0.75/H2 labels contain some useful timestamp/viewpoint information, not as deployable checkpoints.
- The current descriptor-only pseudo-label objective still needs a retention/non-activation constraint. In-batch hard negatives and per-pair caps did not prevent extra low-precision matches in numeric/viewpoint. A future attempt should add explicit negative/abstention supervision from current route zero/low-support pairs, or train/evaluate a score gate that preserves current precision before allowing extra matches.
- `pfm_pytorch_training.py` gradient accumulation is memory-expensive because it accumulates loss graphs and only calls backward after the accumulation loop. For 1024 cache PFM descriptor training on 32GB, `batch_pairs=8, gradient_accumulation_steps=2` OOMs, while `batch_pairs=2, gradient_accumulation_steps=1` is safe. Future large-batch training should either reduce micro-batch size or refactor accumulation to backward per micro-batch.
- Warp-aware hard negatives are not sufficient to make P1 descriptor retention routeable. The P1 warp-negative run produced almost the same sparse behavior as the earlier descriptor-only P1 run: numeric/viewpoint full-val still over-activated from 836 baseline matches to 2702 matches and precision fell to 0.351221; timestamp/viewpoint full-val stayed weakly positive at 81/368, but fixed64 remained below baseline at 16/92.
- Existing training knobs are point-level descriptor/keypoint losses, not route-level abstention supervision. `pfm_pytorch_training.py` has in-batch hard negatives, warp-aware hard negatives, and pseudo-keypoint heatmap mean regularization, but no direct objective that says a current-route zero/low-support pair should stay inactive unless a confidence gate is met. That gap now blocks further descriptor-positive-only P1 variants from being useful.
- Match visualization must use the synthetic GT warp, not matcher-internal color or homography status, when presenting fixed artificial pairs. The corrected `对比文档/` figures now make this explicit across PFM, SIFT/RootSIFT/ORB/AKAZE, and LightGlue-SIFT: green is GT-correct at <=5 px, red is GT-wrong or invalid. This is important because many external methods have perfect/near-perfect homography inliers, while PFM examples expose wrong lines that were previously hidden by random coloring.
- The fixed 6 x 2 comparison is easy for external matchers after geometric filtering because the cached synthetic transforms are global homographies. On sampled GT points, all 12 fixed pairs fit a homography with median error 0 and 100% <=5 px. Therefore `SIFT/RootSIFT/ORB/AKAZE/LightGlue + homography/RANSAC` can look nearly perfect even for large displacement. The raw matcher view is the fairer diagnostic for visible false matches: external raw precision is 0.838798 with 2232 wrong matches, versus post-RANSAC precision 0.998844 with 13 wrong matches.
- For user-facing matching-effect figures on these synthetic pairs, the default must be raw matcher output with GT-colored lines. RANSAC/Homography filtering is useful as a separate geometry-filtered diagnostic, but it should not be the main visual comparison when the goal is to see each algorithm's own false matches, because it hides most raw red lines.
- The real extreme TIFF pair in `对比文档/极端测试/` must be reported differently from the synthetic fixed pairs. It has no `.pt` warp, no artificial transform metadata, and no人工 GT correspondence file in the project, so green/red correctness coloring would be fabricated. For this case, the correct output is neutral raw-match visualization plus keypoint/match counts only; precision/correct/wrong require a future manual or generated ground-truth annotation.
- On the extreme TIFF pair at long-edge 1600, strict ratio local features output relatively few raw matches (SIFT-r0.80 49, RootSIFT-r0.80 30), while looser/cross-check methods and PFM hit the 256 match cap. Without GT this is not a quality ranking, but it is a useful visual diagnostic: high raw count can include many visually implausible long-range lines, especially when no geometric filter is allowed.
- Mined false-match supervision is implemented and technically works, but it is not enough to fix raw PFM matching. The first P1-positive run and the stronger Agent13 style-specific positive run both improved descriptor retrieval only slightly and did not improve six-group raw sparse precision. The practical interpretation is that point-level descriptor positives/negatives are being diluted by the current analytic texture-blend inference path and by weak learned-descriptor standalone distinctiveness.
- The base checkpoint's learned descriptor is not currently a competitive matcher by itself. In a 32-pair six-group sweep, blend=0 produced only 8 total matches with the current texture-keypoint/min-margin protocol. The model's usable sparse matches come mainly from the analytic rotation-invariant texture descriptor plus keypoint/route calibration, not from a strong deep descriptor comparable to SuperPoint/DISK/LoFTR-style learned features.
- Texture blend weight materially changes the evaluation story. On the same base 32-pair sample, blend=1 had higher total precision than blend=4 (0.237369 vs 0.207492), while blend=4 reduced match support and made learned-descriptor training even less influential. Future descriptor training should either train/evaluate at the same blend setting or add a learnable texture-adapter/projection; otherwise learned updates remain too small to move raw sparse metrics.
- The current PyTorch PFM state files are about 73 MB, not a few MB. Files around 3.3 MB in the project are synthetic pair cache archives, not model weights. The 73 MB state is still smaller than many published deep matchers because the backbone is a compact CNN plus sparse/dense heads and a small graph matcher, with no large pretrained transformer/image encoder and no optimizer state saved.
- A zero-initialized 1x1 texture adapter is safe but too weak. Joint training can move descriptor validation more, but it also damages rotate precision; adapter-only training is stable and slightly positive at blend=4, but the best aggregate gain was only 0.197315 -> 0.197451 on the six fixed test groups. The next model-side attempt should use a higher-capacity but still compatible fusion block, such as learned channel gates over `[learned, texture, learned-texture difference]`, and should include an explicit route/precision regularizer rather than only point-level descriptor losses.
- Increasing descriptor-fusion capacity alone still does not fix PFM. The 3.02M-parameter residual fusion block increased saved state size to 85 MB and kept old checkpoints compatible, but the first 240-step probe produced no raw sparse precision gain. The more immediate bottleneck is now training data scheduling: with `batch_pairs=2`, 103/240 steps were false-only because pseudo-label and false-match pair pools are unioned before sampling. The next high-value code change should separate positive-pair and false-pair sampling quotas so every batch contains positive correspondences and hard negatives, instead of letting mined false-only pairs dominate many steps.
- Separating positive pseudo-label and mined-false quotas is necessary but not sufficient. The low-LR quota-balanced fusion probe removed the false-only-step failure mode, yet raw precision stayed flat because the zero-initialized fusion output barely moved. For zero-output residual adapters, learning rate and batch size must be high enough to make the new branch participate in inference.
- The stronger fusion run is the first descriptor-side model update in this sequence with a clear six-group raw sparse gain. With `batch_pairs=4`, `lr=3e-5`, and separate positive/false quotas, blend=1 improved every group and raised total precision 0.227604 -> 0.235790 while adding 3538 correct matches. This is still far below classical/LightGlue raw baselines, but it is a real learned-PFM improvement rather than a routing or external-fallback gain.
- Texture blend selection remains a core part of the model result. The strong fusion checkpoint should be evaluated/reported with blend=1; blend=4 overweights texture after the fusion update, sharply reducing support and aggregate precision despite a high numeric/rotate score. Future training should either train multiple blend settings explicitly or learn the blend/gating weight instead of assuming the old texture-heavy inference default.
- Current next model-side work should build on `runs/cross_view_1024_descriptor_fusion_quota_stronglr3e5_b4_240_seed1234`, not the earlier low-LR fusion probe. Sensible next probes are controlled longer training from that checkpoint, learned blend/gate regularization, or adding a route/precision-preservation objective; reverting to descriptor-positive-only or tiny adapter-only runs is lower value based on the accumulated evidence.
- Continuing positive/false-only fusion training improves weak viewpoint/compound retrieval but starts to forget rotate. The `continue_lr1e5` run increased total correct matches and validation top1, but timestamp/rotate precision regressed below the strong checkpoint. This confirms rotate should stay in the training mix even when positive pseudo-labels are only available for viewpoint/compound.
- Low-probability supervised quotas plus base synthetic slots are a useful regularizer. The sampler should not fill every batch with pseudo/false pairs whenever both pools are active; using probabilities to leave base slots lets the model retain rotate and generic warp behavior. The effective pattern for 32GB 5090 is `batch_pairs=4`, pseudo probability 0.25, false probability 0.25, and 2 base pairs per step.
- Synthetic regularization has a clear precision/recall tradeoff. `synthetic_loss_weight=0.2` gives the highest raw margin-0.01 precision (0.242761) but loses correct matches, while `synthetic_loss_weight=0.05` gives the best correct-match count (30420) and becomes the best balanced model after a modest inference margin.
- Current recommended pure-PFM version is the `mix005` checkpoint with blend=1 and `min-margin=0.015`: 26902/98207 (0.273932) on the fixed six-group test, versus base 25862/113627 (0.227604). It is the first model/config in this iteration that improves both precision and correct count over the base while reducing wrong matches by more than 16k.
- Do not overstate this as solved. A high-precision mode exists (`mix02`, margin 0.02: 0.317707), but it falls below base correct-match count. The balanced pure-PFM version is now materially better than the starting point, yet still far below classical/hybrid matching; the next architectural improvement should target learned confidence/abstention or learned blend gating rather than another long positive/false descriptor-only continuation.
- The satsim path issue is fixed structurally: the data simulation directory now has `kernel/`, `dem/`, `dom/`, `build/`, and `相机轨迹/`, and `mars_orbit_to_tsai.py` defaults can point inside this directory. Explicit old `/home/xjw/502软件平台/...` paths are no longer needed for the normal data-generation path.
- The required satsim optimization is not primarily throughput; it is supervision export. Adding per-pixel camera-space depth to the existing CUDA z-buffer output is sufficient to reconstruct world points from TSAI and generate exact `warp_a_to_b`/`valid_mask` for PFM training. A separate world-XYZ export is optional, not required for the first cache builder.
- The 2048 depth smoke confirms the geometry chain is numerically consistent. For CameraA self-projection, sampled depth points reproject with max error about 0.0002 px. For CameraA -> CameraA_xp8, around 79% of sampled points are inside the target view and all inside sampled points pass the current depth-consistency check. This validates using depth+TSAI for dense warp generation.
- A 2048 PFM LibTorch pair archive is large but manageable: the first smoke pair is 69 MB and contains 3,544,642 valid source pixels, valid fraction 0.845109. Full-scale generation at 2048 will therefore be storage-heavy; pair count should be controlled by render gap, view pairing rules, and valid-fraction filtering.

## PFM v2.1-full 256 training findings (2026-05-27)
- The local NVMe cache is now synchronized with the 8T source snapshot used for training: train 3735 and val 1863 `pair_*.pt` archives. A second rsync pass was needed because the simulator wrote 9 new train pairs during the first copy.
- The current full model training is a true 256-dimensional run from random initialization. It does not load or widen the earlier 48/192 checkpoint, matching the requirement to重新提取 256 维特征点 rather than map 192 -> 256.
- A concrete wiring issue was found before training: the training descriptor helper still used backbone `features[1]` directly, which bypassed the new v2.1 Dual-FPN split. After fixing it, focused tests and full discovery passed, and CLI training uses the same Dual-FPN/SparseHead path as inference.
- On the 5090 32GB, `training_crop_size=1024`, `batch_pairs=1`, and `samples_per_pair=512` already pushes memory to roughly 31.8GB while keeping GPU utilization near 100%. Increasing batch size at this model size is likely to OOM; if memory becomes unstable, reduce supervised samples before changing descriptor width.
- Early full-256 training is numerically healthy: the 1024 crop smoke completed, and the formal run reached at least step 100 with `skip=0`. Early sampled top1 improved quickly, but this is not sufficient evidence of final matching quality; the decisive check remains the generated visual report and raw/graph matcher metrics after 3000 steps.
- After 3000 steps, the descriptor training signal on the generated validation distribution is extremely strong: eval top1 reaches `0.9985`, and the 16-sample raw descriptor visual report gives `8187/8192` correct matches. This is the first clear evidence that the full 256-dimensional extractor can learn the synthetic pose-cache geometry well.
- The GraphMatcher remains the bottleneck. On the same 16 validation samples, graph matching gives only `6469/8192` correct and drops hard precision to `0.5254`. The immediate interpretation is that the extractor/descriptor is currently ahead of the graph assignment module; route decisions should not assume GraphMatcher improves the result until it is retrained or recalibrated.
- The run was fast because it was step-limited, not epoch-limited: 3000 optimizer steps with `batch_pairs=1` over cached NVMe tensors and random 1024 crops. It is a valid first training pass, but it has not exhausted all 3735 train pairs as full epochs, nor has it proven generalization to real TIFF/cross-height imagery.
- The weak-texture complaint is mainly a candidate selection/reporting problem after the full-256 run. With coverage-aware sampling on the same checkpoint, the raw report doubled requested matches from 8192 to 16384 while keeping precision at `0.999023`; the mean occupied 16x16 grid fraction reached `0.812012`, and weak-texture match fraction reached `0.264160`. This confirms that the descriptor can support many more distributed matches when selection is not dominated by texture peaks.
- Do not train blindly to solve point distribution before checking sampling. The first effective fix is to expose high-texture / weak-texture / uniform quotas plus a per-cell cap in inference/reporting. Backbone or keypoint heatmap training should come after this, only if the coverage-aware selector still leaves weak areas under-supported on harder held-out pairs.
- The coverage continuation did not damage the descriptor. After 1500 low-LR steps from the full-256 checkpoint, validation retrieval improved slightly (`loss 0.022447 -> 0.014582`, `top1 0.998528 -> 0.999054`) and the coverage-aware raw report improved to `16379/16384` correct with weak-texture match fraction `0.270019`. The next risk is not synthetic validation precision, but transfer to fixed six-group and real TIFF cases.
## 2026-05-30 GraphMatcher/弱纹理/光照/旋转诊断发现

- 最近完整训练结果显示 raw descriptor 抽样 precision `0.9161`，GraphMatcher `0.8484`；极端 pair `pair_004541_cross_off008_s00109_s00119.pt` raw 为 `136/512`，GraphMatcher 为 `17/512`。
- `python/pfm_model.py` 中 `PlanetaryGraphMatcher` 已有 `candidate_topk=64` 的 raw descriptor candidate mask，但最终 pair logits 仍然来自 graph-refined embedding dot product + geometry bias，并没有把 raw cosine 作为 residual 主分数。
- GraphMatcher metadata 当前包含 normalized x/y、radius、score、scale、orientation、affine、quality、contrast、uncertainty；极端跨位置中 x/y 或 geometry bias 可能误导。
- `graph_matcher_correspondence_loss()` 当前直接对 GraphMatcher logits 做行/列 CE，没有 residual warmup、no-match/dustbin 负样本指标，也没有单独保护 raw descriptor 的强匹配。
- `scripts/training_visual_report.py` 当前报告匹配 precision、覆盖率、弱纹理比例等，但没有 raw top-K recall、accepted/rejected/dustbin 统计，也没有按数据来源拆分 same/cross/extreme。
- 当前 weak texture 指标只有 match fraction，不足以判断弱纹理分支是否有效；需要输出 weak texture precision、weak texture correct/wrong、weak texture recall/accepted count。
- C4 descriptor 只覆盖 0/90/180/270 离散旋转，不能证明真实任意角度旋转鲁棒；需要连续旋转压力测试。
- illumination/texture branch 需要 same-geometry photometric stress test，否则无法证明它不是装饰性分支。
- 本轮修改后，同一个旧权重在 `pair_004541_cross_off008_s00109_s00119.pt` 上用 residual GraphMatcher full metadata 得到 `133/512`，descriptor-only metadata 得到 `140/512`。这基本确认此前 `17/512` 是 GraphMatcher 覆盖 raw descriptor 主信号造成的。
- 该 pair 新报告中 raw top-K recall：top1 `0.901163`、top5 `0.988372`、top16 `0.988372`、top32 `0.994186`、top64 `0.994186`，说明真实匹配大多在 raw top-K 候选内，下一步应该训练 rerank/filter，而不是优先大改 extractor。
- 该 pair weak texture precision 为 `0.486891`，weak texture correct 为 `130/267`；弱纹理区域确实被覆盖，但准确率仍低，后续需要 hard negatives 或 semi-dense fallback。

- 2026-05-30 GraphMatcher-only residual 3epoch 训练结论：普通 same_position 有提升，但指定 extreme pair `pair_004541_cross_off008_s00109_s00119.pt` 从 raw `116/512` 掉到 graph `77/512`，weak texture precision 从 `0.3846` 掉到 `0.2962`；只用正样本 CE 会继续压坏极端跨位置。
- 下一步已改为 no-match/dustbin + metadata 消融训练。训练侧现在可用 `descriptor_only/no_xy/no_geometry/no_quality` 关闭可能误导的坐标/几何/质量先验，并在每个 pair 中加入远离正样本的 unmatched distractor 点监督 dustbin。

- 2026-05-30 no_xy + dustbin GraphMatcher probe 显著修复了“GraphMatcher 压坏 descriptor”的问题。指定极端样本从上一轮正样本 CE 训练后的 `77/512=0.1504`，变为 `89/106=0.8396`；它不是提高了召回，而是正确学会大量拒配。
- no-match/dustbin 监督比单纯正样本 CE 更符合正式匹配需求：18 样本 GraphMatcher micro precision 达 `0.9903`，极端组 micro precision 达 `0.9834`，但 accepted matches 从 raw 的 `9216` 降到 `7973`，指定极端样本从 `512` 降到 `106`。下一步优化目标应是保持 high precision 的同时提高 accepted recall。
- 去掉 x/y metadata 先验是有效的安全默认。极端跨位置中坐标先验很容易误导 matcher；当前建议 GraphMatcher 训练/评估默认先用 `no_xy + dustbin negatives`，后续再通过消融决定是否逐步恢复几何/质量 metadata。
- 召回校准 probe 证明降低 dustbin 训练强度会迅速损害 precision：`nm64/w0.2` 让 18 样本 accepted `7973 -> 8607`、correct `7896 -> 8174`，但 micro precision `0.9903 -> 0.9497`；指定极端样本 `106/89/0.8396 -> 176/110/0.6250`。这不是合格的主模型替代，只适合作为 precision/recall tradeoff 参考。
- 当前最佳模型选择应优先保留 `nm128/w0.5 no_xy dustbin` checkpoint。下一步高价值方向不是继续训练降低 dustbin，而是报告/推理层的 calibration：例如按 confidence margin、raw top-K rank、weak texture quality 分层接收，或输出 high_precision / balanced 两档阈值。
